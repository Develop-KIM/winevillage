<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.winevillage.winevillage.store.IStoreService">
	<select id="getStores" resultType="com.winevillage.winevillage.store.StoreDTO">
       SELECT
            Store_code, Store_sido, Store_gugun, Store_name, Store_url, Store_explain, Store_tel, Store_addr, latitude, longitude
        FROM
            WineStores
    </select>
</mapper>