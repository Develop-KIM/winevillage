<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.winevillage.winevillage.product.ProductService">
	<select id="getTotalCount" resultType="int"
	parameterType="com.winevillage.winevillage.product.ParameterDTO">
		SELECT COUNT(*) FROM product
		<if test="searchKeyword!=null and !searchKeyword.equals('')">
		WHERE ${searchField} LIKE '%'||#{searchKeyword}||'%'
		</if>
	</select>
	
	<select id="listPage"
		resultType="com.winevillage.winevillage.product.ProductDTO"
		parameterType="com.winevillage.winevillage.product.ParameterDTO">
		SELECT * FROM (
			SELECT Tb.*, rownum rNum FROM (
				SELECT * FROM product
				<if test="searchKeyword!=null and !searchKeyword.equals('')">
				WHERE ${searchField} LIKE '%'||#{searchKeyword}||'%'
				</if>
				ORDER BY productno DESC
			)Tb
		)
		WHERE rNum<![CDATA[>=]]>#{start} AND rNum<![CDATA[<=]]>#{end}
	</select>
	
	<insert id="productWrite"
	parameterType="com.winevillage.winevillage.product.ProductDTO">
	INSERT INTO product (productNo, productCode, productName, productInfo, productImg,
		fullPrice, discountPrice, stock, discountRate, wine, country, grapeVariety, alcohol, body,
		acidity, tannins, sweetNess)
	VALUES (#{product_seq}, #{productCode}, #{productName}, #{productInfo}, #{productImg},
	 	#{fullPrice}, #{discountPrice}, #{stock}, #{discountRate}, #{wine}, #{country}, #{grapeVariety}, #{alcohol}, #{body},
	 	#{acidity}, #{tannins}, #{sweetNess})
	</insert>

</mapper>













