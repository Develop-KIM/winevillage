<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.winevillage.winevillage.product.ProductService">
	<select id="getTotalCount" resultType="int"
		parameterType="com.winevillage.winevillage.product.ParameterDTO">
		SELECT COUNT(*) FROM product
		<if test="searchKeyword!=null and !searchKeyword.equals('')">
			WHERE ${searchField} LIKE '%'||#{searchKeyword}||'%'
		</if>
	</select>

	<select id="listPage"
		resultType="com.winevillage.winevillage.product.ProductDTO"
		parameterType="com.winevillage.winevillage.product.ParameterDTO">
		SELECT * FROM (
		SELECT Tb.*, rownum rNum FROM (
		SELECT * FROM product
		<if test="searchKeyword!=null and !searchKeyword.equals('')">
			WHERE ${searchField} LIKE '%'||#{searchKeyword}||'%'
		</if>
		ORDER BY productno DESC
		)Tb
		)
		WHERE rNum<![CDATA[>=]]>#{start}
		AND rNum<![CDATA[<=]]>#{end}
	</select>
	<select id="Wine_count" resultType="int"
		parameterType="com.winevillage.winevillage.product.ParameterDTO">
		SELECT COUNT(*) FROM product Where state IS NOT NULL
	</select>
	<select id="listWine"
    resultType="com.winevillage.winevillage.product.ProductDTO"
    parameterType="com.winevillage.winevillage.product.ParameterDTO">
    SELECT * FROM (
        SELECT Tb.*, ROWNUM AS rnum FROM (
            SELECT * FROM product
            WHERE wine = 
                <choose>
                	<when test="wineType == '레드'">
                		'레드'
                	</when>
                    <when test="wineType == '화이트'">
                        '화이트'
                    </when>
                    <when test="wineType == '로제'">
                        '로제'
                    </when>
                    <when test="wineType == '스파클링'">
                        '스파클링'
                    </when>
                </choose>
            ORDER BY productno DESC
        ) Tb
    )
	WHERE rNum<![CDATA[>=]]>#{start}
	AND rNum<![CDATA[<=]]>#{end}
	</select>
	
	<insert id="productWrite"
		parameterType="com.winevillage.winevillage.product.ProductDTO">
		INSERT INTO product (productNo, productCode, productName, productInfo,
		productImg,
		fullPrice, discountPrice, stock, discountRate, wine,
		country, grapeVariety, alcohol, body,
		acidity, tannins, sweetNess)
		VALUES (product_seq.nextval, #{productCode}, #{productName},
		#{productInfo}, #{productImg},
		#{fullPrice}, #{discountPrice}, #{stock}, #{discountRate}, #{wine}, #{country},
		#{grapeVariety}, #{alcohol}, #{body},
		#{acidity}, #{tannins}, #{sweetNess})
	</insert>

	<select id="productView"
		resultType="com.winevillage.winevillage.product.ProductDTO"
		parameterType="com.winevillage.winevillage.product.ProductDTO">
		SELECT * FROM product WHERE productNo=#{productNo}
	</select>

	<update id="productEdit"
		parameterType="com.winevillage.winevillage.product.ProductDTO">
		UPDATE product SET
		productCode=#{productCode}, productName=#{productName},
		productInfo=#{productInfo}, productImg=#{productImg},
		fullPrice=#{fullPrice}, discountPrice=#{discountPrice},
		stock=#{stock}, discountRate=#{discountRate},
		wine=#{wine}, country=#{country}, grapeVariety=#{grapeVariety}, alcohol=#{alcohol},
		body=#{body},
		acidity=#{acidity}, tannins=#{tannins}, sweetNess=#{sweetNess}
		WHERE productNo=#{productNo}
	</update>

	<delete id="delete"
		parameterType="com.winevillage.winevillage.product.ProductDTO">>
		DELETE FROM product WHERE productNo=#{productNo}
	</delete>
</mapper>













