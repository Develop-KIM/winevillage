<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.winevillage.winevillage.member.MemberService">
    
    <!-- 회원 정보 삽입 -->
    <insert id="insert" parameterType="com.winevillage.winevillage.member.MemberDTO">
        INSERT INTO member (memberId, password, phoneNumber, email, address, birthday)
        VALUES (#{memberId}, #{password}, #{phoneNumber}, #{email}, #{address}, #{birthday})
    </insert>
    
    <!-- 전화번호 중복 확인 -->
    <select id="checkPhoneNumberExists" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM member WHERE phoneNumber = #{phoneNumber}
    </select>
    
	<select id="memberTotalCount" resultType="int"
	parameterType="com.winevillage.winevillage.member.ParameterDTO">
		SELECT COUNT(*) FROM member
		<if test="searchKeyword!=null and !searchKeyword.equals('')">
		WHERE ${searchField} LIKE '%'||#{searchKeyword}||'%'
		</if>
	</select>

	<select id="memberlistPage"
		resultType="com.winevillage.winevillage.member.MemberDTO"
		parameterType="com.winevillage.winevillage.member.ParameterDTO">
		SELECT * FROM (
			SELECT Tb.*, rownum rNum FROM (
				SELECT * FROM member
				<if test="searchKeyword!=null and !searchKeyword.equals('')">
				WHERE ${searchField} LIKE '%'||#{searchKeyword}||'%'
				</if>
				ORDER BY memberNo DESC
			)Tb
		)
		WHERE rNum<![CDATA[>=]]>#{start} AND rNum<![CDATA[<=]]>#{end}
	</select>
</mapper>