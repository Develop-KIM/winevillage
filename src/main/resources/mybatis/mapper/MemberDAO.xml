<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.winevillage.winevillage.member.MemberMapper">
    
    <!-- 회원 정보 삽입 -->
    <insert id="insert" parameterType="com.winevillage.winevillage.member.MemberDTO">
        INSERT INTO member (name, memberId, password, phoneNumber, email, postcode, address1, address2, birthday)
        VALUES (#{name}, #{memberId}, #{password}, #{phoneNumber}, #{email}, #{postcode}, #{address1}, #{address2}, #{birthday})
    </insert>
    
    <!-- 전화번호 중복 확인 -->
    <select id="checkPhoneNumberExists" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM member WHERE phoneNumber = #{phoneNumber}
    </select>
    
    <!-- 아이디 중복 확인 -->
    <select id="checkMemberIdExists" parameterType="String" resultType="int">
    	 SELECT COUNT(*) FROM member WHERE memberId = #{memberId}
    </select>
</mapper>

