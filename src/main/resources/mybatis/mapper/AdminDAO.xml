<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.winevillage.winevillage.jdbc.AdminService">
	<insert id="adminInsert"
		parameterType="com.winevillage.winevillage.jdbc.AdminDTO">
		INSERT INTO admin (admin_id, admin_name, admin_pass,
				admin_email, admin_phone)
		VALUES (#{admin_id}, #{admin_name}, #{admin_pass},
				#{admin_email}, #{admin_phone})
	</insert>

</mapper>
